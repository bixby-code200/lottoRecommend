action (PickLotto) {
  description (로또 번호를 뽑는다.)
  collect {
    input (numberOfPick) {
      type (NumberOfPick)
      min (Optional) max (One)
    }
    input (addNumber) {
      type (AddNumber)
      min (Optional) max (Many)
    }
  }
  type(Search)
  output (Result)
}
